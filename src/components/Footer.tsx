import { styled } from "@mui/material";

const Footer = () => {
  return <FooterContainer>React Compiler Test</FooterContainer>;
};

const FooterContainer = styled("div")`
  color: white;
  font-weight: 900;
`;

export default Footer;
